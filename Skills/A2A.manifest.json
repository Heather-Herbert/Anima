{
  "name": "A2A",
  "description": "Agent-to-Agent learning and discovery skill",
  "type": "skill",
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "learn_from_agent",
        "description": "Connect to another Anima or OpenClaw instance to learn its identity and soul.",
        "parameters": {
          "type": "object",
          "properties": {
            "endpoint": { "type": "string", "description": "The OpenAI-compatible chat completion URL (e.g. http://localhost:18701/v1/chat/completions)" },
            "apiKey": { "type": "string", "description": "Optional API Key or Bearer Token" },
            "agentId": { "type": "string", "description": "Optional agent identifier" }
          },
          "required": ["endpoint"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "delegate_task",
        "description": "Send a specific task or question to another Anima or OpenClaw instance for collaboration.",
        "parameters": {
          "type": "object",
          "properties": {
            "endpoint": { "type": "string", "description": "The OpenAI-compatible chat completion URL of the target agent" },
            "task": { "type": "string", "description": "The specific task or question to delegate" },
            "apiKey": { "type": "string", "description": "Optional API Key or Bearer Token" },
            "agentId": { "type": "string", "description": "Optional agent identifier" }
          },
          "required": ["endpoint", "task"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "discover_agents",
        "description": "Discover active Anima or OpenClaw agents on the local network using UDP broadcast and port scanning.",
        "parameters": {
          "type": "object",
          "properties": {
            "useUdp": { "type": "boolean", "description": "Use UDP broadcast for fast discovery. Defaults to true." },
            "startPort": { "type": "integer", "description": "Fallback port to start scanning from (default 18700)" },
            "endPort": { "type": "integer", "description": "Fallback port to end scanning at (default 18790)" },
            "scanSubnetOnly": { "type": "boolean", "description": "If true, only scan the local /24 subnet. If false, scan all private IP ranges (slow). Defaults to true." }
          }
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "manage_peers",
        "description": "List, approve, or deny agent-to-agent pairing requests and manage trusted peers.",
        "parameters": {
          "type": "object",
          "properties": {
            "action": { "type": "string", "enum": ["list", "approve", "deny", "remove"], "description": "Action to perform." },
            "id": { "type": "string", "description": "Agent identifier for approve/deny/remove actions." }
          },
          "required": ["action"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "get_local_endpoint",
        "description": "Get your own A2A endpoint and Agent ID to share with other agents for pairing.",
        "parameters": {
          "type": "object",
          "properties": {}
        }
      }
    }
  ]
}
